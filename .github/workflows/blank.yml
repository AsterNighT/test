name: ci

on:
  pull_request:
    branches:
      - master
      - release-*

jobs:

  pull:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        job:
          - verify
          - build
          - test
    steps:
      - name: test
        run: echo help
